# Docker

开源容器平台,本身只能在linux上运行(需要linux内核)

## Compose

编排

`docker compose up` cmd命令

### VS 容器

有两个选项

*   容器支持 把不是docker的项目变成docker启动的项目
*   容器编排支持 把多个docker项目编排,生成一个新项目

`docker-compose.yml` 自动生成的基础配置

`docker-compose.override.yml` 在基础配置上修改的配置部分

配置放在`Properties`->`launchSettings.json`中
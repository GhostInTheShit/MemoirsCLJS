---
categories: WPF
---

# Video

视频

## MediaElement

`Source` 设置视频源就可以了.

`Stretch` 拉伸模式,设置为`Uniform`等比例拉伸

`LoadedBehavior="Manual"` 手动播放,默认会一加载就播放.

控制和Audio一样.

`MediaOpened` 资源打开事件,这个时候已经能得到`NaturalDuration`

`MediaEnded` 视频播放结束事件
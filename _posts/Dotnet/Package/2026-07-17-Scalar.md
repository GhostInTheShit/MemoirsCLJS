# Scalar 

Swagger的替代品,OpenAPI的UI实现

需要用到OpenAPI的数据.

`Microsoft.AspNetCore.OpenApi` 微软的OpenAPI包

`Scalar.AspNetCore` Scalar包

```C#
//在buider里面添加OpenApi
builder.Services.AddOpenApi();

var app = builder.Build();

//在app里启用OpenApi
app.MapOpenApi();
//启用Scalar
app.MapScalarApiReference();
```

---
categories: C#
---

# DriveInfo


## 属性

`Name` 盘名

`DriveType` 盘类型

`DriveFormat` 盘格式

`TotalSize` 总容量

`AvailableFreeSpace` 可用控件

`VolumeLabel` 卷标

## Method

`GetDrives()` 获得所有逻辑盘,是个`DriveInfo[]`
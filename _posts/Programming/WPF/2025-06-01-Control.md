---
categories: WPF
---

# Control

控件分为布局控件(Panel)和内容控件(Control)

------------------------------------------------------------------------
## Window

本质也是一个Control

`WindowStartupLocation` 属性,出现的位置

`ContentRendered` 事件，UI刚渲染完

### 模拟对话框
`ShowDialog()` 以模拟窗口的方式打开.有返回值

>[!warning]
>只能调用一次(每次都new一个就行),必须从 UI 线程调用

`DialogResult` 可空布尔值,需要模拟`ShowDialog`打开,一旦设置true或false会触发Closing事件关闭窗口并返回值.

也可以用 `IsDefault` `IsCancel` 来简化
```xml
<!-- 按下 Enter 自动触发 -->
<Button IsDefault="True" Click="OkButton_Click">确定</Button>

<!-- 按下 Esc 自动触发 -->
<Button IsCancel="True" Click="CancelButton_Click">取消</Button>
```

调用一般在后置代码,通过`button`打开

## Menu
菜单
```xml
<DockPanel>
        <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                        <MenuItem Header="_New" Click="MyMethod"></MenuItem>
                        <Separator/>
                        <MenuItem Header="_Exit"></MenuItem>
                </MenuItem>
                <MenuItem Header="_Tools">
                        <MenuItem Header="Manager">
                                <MenuItem.Icon>
                                                <Image Source=""/>
                                </MenuItem.Icon>
                        </MenuItem>
                </MenuItem>
        </Menu>
        <TextBox> something </TextBox>
</DockPanel>
```

`<MenuItem Command="Cut"></MenuItem>` 也可以放命令

### ContextMenu
右键单击出现的菜单,和Menu差不多，都是继承MenuBase类.
```xml
<StackPanel>
        <Button Content="Button">
                <Button.ContextMenu>
                        <ContextMenu>
                                <MenuItem Header="Menu1"></MenuItem>
                        </ContextMenu>
                </Button.ContextMenu>
        </Button>
</StackPanel>
```

或者使用后置代码  
放到资源里面
```xml
<Window.Resources>
    <ContextMenu x:Key="cmButton">
        <MenuItem Header="Menu item 1" />
    </ContextMenu>
</Window.Resources>
<Grid>
    <Button Content="Click me!" VerticalAlignment="Center" HorizontalAlignment="Center" Click="Button_Click" />
</Grid>
```

```c#
private void Button_Click(object sender, RoutedEventArgs e)
{
    ContextMenu cm = this.FindResource("cmButton") as ContextMenu;
    //菜单的目标位置
    cm.PlacementTarget = sender as Button;
    cm.IsOpen = true;
}
```

## ToolBar

工具栏可以自动处理溢出，当项目放不下时候会自动放到箭头里面.
可以用`ToolBar.OverflowMode`来设置优先级.

`ToolBarTray`是整个工具栏的容器,`Orientation`默认为`Horizontal`

```xml
<ToolBarTray DockPanel.Dock="Top" Orientation="Horizontal">
        <ToolBar>
                <Button Command="New" Content="New" ToolBar.OverflowMode="Always"></Button>
                <Button Command="Open" Content="Open"></Button>
        </ToolBar>            
        <ToolBar>
                <Button Command="Cut" ToolTip="This is Cut">
                        <Image Source="SomeImg"></Image>
                </Button>
        </ToolBar>                    
</ToolBarTray>
```

几乎可以放任何控件,而不是只有`Button`

## StatusBar 状态栏

```xml
<StatusBar DockPanel.Dock="Bottom">
        <StatusBarItem>
                <TextBox >Char:</TextBox>
        </StatusBarItem>
</StatusBar>
```

## 功能区控件

Ribbon 没有内置

## Rich Text 富文本

流体系,和UI体系有所不同

自带`Ctrl+F`搜索,需要窗口有足够的空间.

`FlowDocument` 需要放到以下三个包装器里面,只读

* FlowDocumentScrollViewer
* FlowDocumentPageViewer
* FlowDocumentReader

----------------------

`FlowDocumentScrollViewer` 滚动条

`ScrollViewer.VerticalScrollBarVisibility`

-------------------------------
`FlowDocumentPageViewer` 自动分页

--------------------------
`FlowDocumentReader` 最全能的,资源消耗最大

ViewingMode - 控制初始查看模式。 默认值为Page，但如果您需要其他默认视图，则可以将其更改为Scroll或TwoPage。 除非明确禁用，否则最终用户仍可以更改此设置。

IsFindEnabled - 使您能够禁用文档中的搜索。 如果禁用，搜索按钮将从工具栏中删除。

IsTwoPageViewEnabled， IsPageViewEnabled 和 IsScrollViewEnabled - 允许您关闭阅读器的特定查看模式。 设置为false时，此模式不再适用于阅读器，并且该按钮将从工具栏中删除。

Zoom - 允许您设置默认缩放级别。 标准是100％，但您可以使用Zoom属性更改此值。

------------------
`FlowDocument` 通常只是只读的,但将其放入一个RichTextBox控件后可以编辑.默认有全选和复制.还可以`Ctrl+滚轮`放大缩小.

`IsOptimalParagraphEnabled="True"` 启用最佳段落布局.

`IsHyphenationEnabled="True"` 启用字段连字符或者断字布局

`<BlockUIContainer>` 可以让FlowDocument放入任何UI控件,原本几乎只能放和文本有关的控件，块级

`<InlineUIContainer>` 行级

----------------------------

`RichTextBox` 控件,可写控件

```C#
//不是简单的textbox,需要TextRange获得值
//rtbEditor 为Xaml创建的RichTextBox
TextRange textRange = new TextRange(rtbEditor.Document.ContentStart, rtbEditor.Document.ContentEnd);
```

----------------------------
使用后置代码创建

```Cs
FlowDocument doc = new FlowDocument();

Paragraph p = new Paragraph(new Run("Hello, world!"));
p.FontSize = 36;
doc.Blocks.Add(p);

p = new Paragraph(new Run("The ultimate programming greeting!"));
p.FontSize = 14;
p.FontStyle = FontStyles.Italic;
p.TextAlignment = TextAlignment.Left;
p.Foreground = Brushes.Gray;
doc.Blocks.Add(p);

//fdViewer为xaml创建的FlowDocumentScrollViewer
fdViewer.Document = doc;
```


## Panel

**布局控件的Background属性没有值时，是不能引发鼠标相关事件的。**

*   `Canvas` 你必须手动移动子控件以保证他们按照你需要的位置和方式排列。推荐在你想要完全自己布置子控件时选用

*   `WrapPanel` 面板将每个子控件按照水平（默认方式）或者竖直的方式满布一行或一列，让后再布置下一行或者下一列。当你需要水平或者竖直排列子控件且能自动滚动进入下一行（列）时采用他。

*   `StackPanel` 拉伸每个项以占据整个 StackPanel 的宽度或高度。当您希望控件列表占据所有可用空间而不换行时，请使用StackPanel。

*   `DockPanel` DockPanel允许您将子控件停靠在顶部、底部、左侧或右侧。默认情况下，如果没有给定特定的dock位置，最后一个控件将填充剩余的空间。

*   `Grid` Grid可以包含多行和多个列。您为每行定义一个高度，为每列定义一个宽度，以像素的绝对数量、可用空间的百分比或自动方式，其中行或列将根据内容自动调整其大小。

*   `UniformGrid` UniformGrid就像Grid一样，具有多行和多列的可能性，但有一个重要的区别：所有行和列将具有相同的大小！当您需要网格行为而不需要为行和列指定不同的大小时

### Canvas

它默认不会做任何事,只是允许你把控件放在里面
```xml
<!--允许你使用Canvas锚点-->
<Canvas>
    <Button Canvas.Left="10">Top left</Button>
    <Button Canvas.Right="10">Top right</Button>
    <Button Canvas.Left="10" Canvas.Bottom="10">Bottom left</Button>
    <Button Canvas.Right="10" Canvas.Bottom="10">Bottom right</Button>
</Canvas>
```    
`Panel.ZIndex` 设置谁先谁后,大的在上.如果不设置或者相同，后面声明的控件在上.

### WrapPanel

水平放置的时候同一行高度相同   
垂直放置的时候同一列宽度相同

### StackPanel

与WrapPanel相似但不会换行，默认是垂直分布，子类默认是拉伸的

### Grid

```xml
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition Height="2*" />
        <RowDefinition Height="1*" />
    </Grid.RowDefinitions>
    <Button Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2">Button</Button>
</Grid>
```
ColumnDefinitionCollection 类型的ColumnDefinitions属性

Grid.ColumnSpan 跨列属性

RowDefinitionCollection 类型的RowDefinitions属性

Grid.RowSpan 跨行属性

ShowGridLines显示网格（但是是虚线,可以使用Border的来显示边框)

`<GridSplitter HorizontalAlignment="Stretch" />` 分隔,自动分辨是垂直还是水平分隔.`ResizeDirection`强制分隔模式

### DockPanel

DockPanel使得在所有四个方向（顶部、底部、左侧和右侧）都可以很容易地停靠内容,
DockPanel内的最后一个元素（除非该特性被特别禁用）将自动填充剩余的空间

```xml
<DockPanel>
    <!--DockPanel.Dock是个附加属性,其实是DockPanel提供的-->
    <Button DockPanel.Dock="Left">Left</Button>
    <Button DockPanel.Dock="Top">Top</Button>
    <Button DockPanel.Dock="Right">Right</Button>
    <Button DockPanel.Dock="Bottom">Bottom</Button>
    <Button>Center</Button>
</DockPanel>
```

`<DockPanel LastChildFill="False">` 禁用最后一个填充

>[!warning]
>如果只有一个元素，那么它会填满控件.

### Viewbox

自动缩放(等比例)内部的控件,用来填满可用空间.

如果不想等比例缩放.`Stretch="Fill"` 一般不推荐,默认为`Uniform`.



------------------------------------------------------------------------

## Control

Control 基类

提供了一个ControlTemplate类型的Template属性,用来给子类实现.

### ContentControl

public class ContentControl : Control, IAddChild

Control的子类

有一个DataTemplate类型的ContentTemplate属性(内容模板)



### ItemsControl

ItemsControl类, 集合控件

public class ItemsControl : Control, IAddChild, IGeneratorHost,
IContainItemStorage

有ItemCollection类型的Items属性

DataTemplate类型的ItemTemplate属性

---------------------------------------------

Label 标签 **可以聚焦到相关的控件**

```xml
<!--名字前下划线,然后指定目标元素,alt+N就能聚焦到Tbox,N为_Name的首字母-->
<Label Content="_Name" Target="{Binding ElementName=Tbox}"></Label>
<TextBox x:Name="Tbox"></TextBox>

<!--如果Label里面有多个元素,要用到AccessText-->
<Label Target="{Binding ElementName=Tbox}">
    <StackPanel>
        <Image Source="some.png" />
        <AccessText Text="_Name:" />
    </StackPanel>
</Label>
<TextBox Name="Tbox" />
```

TextBlock 只读文本



------------------------
TextBox 可以编辑文本  
`AcceptsReturn` 接受换行,  
`SpellCheck.IsEnabled="True" Language="en-US"` 拼写检查，以及Language属性

`SelectionChanged` 事件
```xml
<TextBox SelectionChanged="TextBox_SelectionChanged" DockPanel.Dock="Top" />
```
```C#
private void TextBox_SelectionChanged(object sender, RoutedEventArgs e)
{
    TextBox textBox = sender as TextBox;
    //txtStatus为另一个UI控件用来显示
    txtStatus.Text = "Selection starts at character #" + textBox.SelectionStart + Environment.NewLine;
    txtStatus.Text += "Selection is " + textBox.SelectionLength + " character(s) long" + Environment.NewLine;
    txtStatus.Text += "Selected text: '" + textBox.SelectedText + "'";
}

// SelectionStart，它给出了当前光标位置或是否有选择：它从什么位置开始。
// SelectionLength，它给出了当前选择的长度，如果有的话。 否则它将返回0。
// SelectedText，如果有选择，它会给我们当前选择的字符串。 否则返回一个空字符串。
```



-----------------------
PassWordBox 密码框

`PasswordChar` 密码字符,默认为点

`MaxLength` 最大密码数

RadioButton 单选按钮

默认为一组,只能一个被选,但是可以分组

`GroupName` 一组的只能被选一个

CheckBox 选择按钮，复选

`IsChecked` 默认选定

`IsThreeState` 三种形态,默认为真(钩子)与假(空),第三种为不定态(正方形)

ComboBox 单选列表

ListBox 每行只能显示一个数据项,可以设置单选和多选

ListView 每行可以显示多个数据项，数据列表，主要为展现数据.

DataGrid 强大的数据表格，可以编辑，选择等待

### UserControl

ClipToBound



### TabControl

包含几个TabItem.


Button

内部的文字内容会被转化为TextBlock,因此你可以按照TextBlock的方式控制.

内部只能接受一个控件,但是可以嵌套

--------------------
Image

`Source` 图片源

绝对路径，用的少，除非是引用其他程序集的  
`<Image Source="/WpfTutorialSamples;component/Images/google.png" />` 第一个指程序集,第二个为路径

相对路径  
`<Image Source="/Images/google.png" />` 更为简单

动态加载,不是预先准备的，比如用户上传图片,要在C#代码中编写

```c#
private void ButtonBase_OnClick(object sender, RoutedEventArgs e)
{
    //创建一个文件对话框
    OpenFileDialog openFileDialog = new();
    //openFileDialog.ShowDialog()为显示窗体，并且接受你是否点击了确认。
    if (openFileDialog.ShowDialog() == true)
    {
        //openFileDialog.FileName为你点击的文件的完整文件名
        var fileName = openFileDialog.FileName;
        Console.WriteLine(fileName);//输出 C:\Users\YouKnowWho\Desktop\atlas.png
        
        Uri uri = new(fileName);
        Console.WriteLine(uri);//输出 file:///C:/Users/YouKnowWho/Desktop/atlas.png

        Image.Source = new BitmapImage(uri);


        //如果为程序内部的资源,直接传递Uri,要加上UriKind.Relative因为是相对路径
        //Uri resourceUri = new Uri("/Images/white_bengal_tiger.jpg", UriKind.Relative);
        //imgDynamic.Source = new BitmapImage(resourceUri);    
    }
}
```
`stretch` 拉伸

*   Uniform: 这是默认模式。 图片将自动缩放，将保留图片的宽高比。
*   UniformToFill: 图片将被缩放，以便完全填充图片区域。 将保留图片的宽高比。
*   Fill: 图片将缩放以适合图片控件的区域。 可能无法保留宽高比，因为图片的高度和宽度是独立缩放的。
*   None: 如果图片小于图片控件，则不执行任何操作。 如果它比图片控件大，则会裁剪图片以适合图片控件，这意味着只有部分图片可见。

Tooltip控件，ToolTipService类

可以作为属性直接用
```xml
<Button ToolTip="Click here and something will happen!">Click here!</Button>
```
也可以嵌套,添加控件
```xml
<Button>
    <Button.ToolTip>
        <StackPanel>
            <TextBlock FontWeight="Bold" FontSize="14" Margin="0,0,0,5">Open file</TextBlock>
            <TextBlock>
            Search your computer or local network
            <LineBreak />
            for a file and open it for editing.
            </TextBlock>
            <Border BorderBrush="Silver" BorderThickness="0,1,0,0" Margin="0,8" />
            <WrapPanel>
                <Image Source="/WpfTutorialSamples;component/Images/help.png" Margin="0,0,5,0" />
                <TextBlock FontStyle="Italic">Press F1 for more help</TextBlock>
            </WrapPanel>
        </StackPanel>
    </Button.ToolTip>
</Button>
```

## MessageBox

消息框

```C#
MessageBox.Show(
//文本
"Hello, world?", 
//标题
"My App", 
//信息框按钮
MessageBoxButton.YesNo, 
//信息框图片
MessageBoxImage.Question, 
//默认被选择的按钮
MessageBoxResult.No);
```

返回值的应用

```C#
//有返回值
MessageBoxResult result = MessageBox.Show(
//第一个是content
"Would you like to greet the world with a \"Hello, world\"?",
//标题 
"My App",
//消息按钮 
MessageBoxButton.YesNoCancel);

switch(result)
{
    case MessageBoxResult.Yes:
        MessageBox.Show("Hello to you too!", "My App");
        break;
    case MessageBoxResult.No:
        MessageBox.Show("Oh well, too bad!", "My App");
        break;
    case MessageBoxResult.Cancel:
        MessageBox.Show("Nevermind then...", "My App");
        break;
}
```

## 文件对话框 OpenFileDialog

```C#
//创建一个文件对话框
OpenFileDialog openFileDialog = new OpenFileDialog();

//是否可以多选
openFileDialog.Multiselect = true;
//文件的类型
openFileDialog.Filter = "Text files (*.txt)|*.txt|All files (*.*)|*.*";
//默认路径，使用了环境类
openFileDialog.InitialDirectory = Environment.GetFolderPath(Environment.SpecialFolder.MyDocuments);
	
//openFileDialog.ShowDialog()是可空布尔类型,当用户点击打开时候会为true
if(openFileDialog.ShowDialog() == true)
{
    //openFileDialog.FileName 如果是单选,文件名
    //openFileDialog.FileNames 多选的文件名,是一个集合
}
```
>[!Warning]
>在处理文件和IO任务时候,你应该要注意引发的异常问题.

## 保存文件对话框

```C#
SaveFileDialog saveFileDialog = new SaveFileDialog();

saveFileDialog.Filter = "Text file (*.txt)|*.txt|C# file (*.cs)|*.cs";

saveFileDialog.InitialDirectory = Environment.GetFolderPath(Environment.SpecialFolder.MyDocuments);

if(saveFileDialog.ShowDialog() == true)
    File.WriteAllText(saveFileDialog.FileName, txtEditor.Text);
```

>[!important]
>`AddExtension` - 默认为true，并确定SaveFileDialog是否应自动为文件名附加扩展名（如果用户省略）。扩展名将基于所选的过滤器，除非这是不可能的，在这种情况下，它将回退到DefaultExt属性（如果指定）。如果您希望应用程序能够保存没有文件扩展名的文件，则可能必须禁用此选项。  
`OverwritePrompt` - 默认为true，并确定如果用户输入的文件名将导致现有文件被覆盖，  SaveFileDialog - 是否应该要求确认。除非在非常特殊的情况下，您通常希望启用此选项。   
`Title` - 如果您想在对话框中使用自定义标题，则可以覆盖此属性。它默认为“另存为”或本地化的等效项，该属性对OpenFileDialog也有效。   
`ValidateNames` - 默认为true，除非它被禁用，否则它将确保用户在允许用户继续之前只输入有效的Windows文件名。

## GroupBox

组合框,可以设置Header,可以快速开发登录界面和注册界面,只能接受一个子控件.  
Header可以用标签代替,内嵌Image.

## Border

属性

Background

BorderBrush

BorderThickness

CornerRadius 圆角

Background 背景,可以用LinearGradientBrush渐变

## Slider

滑动条,可以和`TextBox`联合使用

属性

**value** 值

Maximum 最大值

TickPlacement 刻度位置

TickFrequency 刻度频率

IsSnapToTickEnabled 扣上刻度(不支持值在刻度之间)

**ValueChanged** 值改变事件

## ProgressBar 

进度条,可以用Grid将Textbox重合,显示文本(百分比).

Minimum

Maximum

**Value** 值

**IsIndeterminate** 不确定进度条

## Calender

日历.

## WebBrowser

内置的浏览器,不太好用,会频繁的弹框问你需不需要安装脚本,而且版本很低,某些页面会拒绝.

内置的3个Command
*   BrowseBack
*   BrowseForward
*   GoToPage

方法

*   WebB.CanGoForward
*   WebB.GoForward()
*   WebB.CanGoBack
*   WebB.GoBack()
*   **WebB.Navigate(url)** 导航到url,需要验证url,不合格会报错(需要`http://`或者(`https://`开头))

## WindowsFormsHost

让WinForms可以在WPF中使用,没看.
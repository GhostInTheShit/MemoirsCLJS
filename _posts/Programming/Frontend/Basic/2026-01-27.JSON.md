---
categories: Json
---

# JSon

JavaScript Object Notation

用来和服务器交互.

和JavaScript的对象写法类似.

```js
//JavaScript 
{Name:"Bob",Age:30,func:function(){}}
```

属性要用`""`包裹,值如果是`string`也要用`""`,不能写方法等复杂类型.
```JSon
//JSON
{"name":"Bob","Age":30}
```

## 方法

`JSON.stringify` 转化为JSON

`JSON.parse` 解析JSON为JS
---
categories: js
---

# JQuery

更简洁简短的js代码.

直接下载然后在`<head>`中引用

`$("#id")` 等同于`document.getElementById("id")` ,以下同理

`$("")`

`$(".class")`

## 元素方法

`$("")` 先获得某个元素

`html` 获得html

`text`

`before()` 在之前创建一个element

`after()` 在之后创建一个element

`prepend()` 创建一个前缀element

`append()` 创建一个后缀element

`insertBefore()` 将现有element放置在前面

`insertAfter()` 将现有element放置在后面

`prependTo()` 将现有element放置在前缀

`appendTo()` 将现有element放置在后缀

`empty()` 将现有element内容设置为空

`remove()` 将现有element去除

## 事件处理

`click()`

`dblclick()`

`mouseover()`

`mouserout()`

`hover()`

`mousemove()`

`focus()`

`blur()`

`keyup()`

`keypress()`

`change()`

-------------

`on()` 订阅事件,**对动态创建的也有用**

```js
$(document).on("eventName","#id",function(){})
```

`off` 取消事件,类似于C#的取消订阅


----------------

`data("key",value)` 储存键值对在某个元素中.临时的


## 效果

淡入/淡出

滑动

显示/隐藏